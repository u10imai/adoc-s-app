-- ============================================================
-- イラストの age_group 変更 記録用テンプレート
-- ============================================================
-- このファイル自体は実行しないでください(テンプレートです)。
--
-- 使い方:
--   1. このファイルの中身をコピーする
--   2. supabase/migrations/ 配下に連番のファイル名で新規保存する
--      例: 0006_update_age_group_<内容がわかる短い名前>.sql
--      (現時点の最新連番は 0005_add_subject_type_and_test_sequence.sql
--       なので、次に作るファイルは 0006〜。実行前に
--       supabase/migrations/ の中身を確認して、既に0006が
--       使われていないか確認すること)
--   3. 下部の「変更内容」を実際の内容に書き換える
--      (日付・対象イラスト・変更前後のage_group・変更理由を必ず書く)
--   4. Supabase SQL Editorに貼り付けて実行する(または `supabase db push`)
--   5. 実行後、このファイルをgit commitしてpushする
--      → git履歴自体が「いつ・何を・なぜ変更したか」の恒久的な記録になる
--      コミットメッセージにも変更理由を書くこと
--
-- age_group に指定できる値(illustrationsテーブルのcheck制約):
--   '年中以下' / '年長' / '小学校低学年' / '小学校高学年' / '中学生以上'
--
-- 対象イラストの特定方法:
--   Supabase Table Editorで illustrations テーブルを開き、
--   image_url(Cloudinary URL)またはcorrect_labelで目的の行を探す。
-- ============================================================

-- ---- 変更内容 (ここを書き換える) ----------------------------
-- 日付: 2026-07-26
-- 対象イラスト: <image_urlまたは特定できる情報>
-- 変更前 age_group: <変更前の値>
-- 変更後 age_group: <変更後の値>
-- 理由: <なぜ変更するのか。評価者の再判定結果など>

update illustrations
set age_group = '<変更後の値>'
where image_url = '<対象イラストのURL>';
