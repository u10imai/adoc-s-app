# 作業ログ — ADOC-S イラスト認知研究アプリ

> パスワード・検査士や子どもの氏名は書かない。
> ※ このリポジトリは **PUBLIC**。追記するときも個人情報・認証情報を入れないこと。
> ※ 2026-09-21 に Mac mini の作業ログと M4airU(MacBook Air)の作業ログを統合した。

## 現在の状況・次にやること・未決事項

(最終更新: 2026-09-21 15:30 JST / M4airU.local)

### 現在の状況

- **フェーズ1(年中・年長、検査士6人が対面実施)は完了。** 2026-09-07 から3日間でデータ取得済み。
- **本番ID**: `001`〜`120` を発行済み(連番は120まで消費、**次の本番発行は `121`**)。
  - `021`〜`120`(9/7発行の100件): 使用済47 / **未使用53** / うち棄却3(024・094・103)
  - `001`〜`020`(8月発行): `020` は旧テストID1230を付け替えて使用済み(小2・小学校低学年、回答55件、アンケート済み、`excluded=false`)。`001`〜`020` の未使用は **8件**。
- **テストID**: 39件、`1201`〜`1240`(**1230は本番020へ移動済みで欠番**)。**次のテストIDは `1241`**(連番 `test_subject_code_seq` = 1240)。
  - 旧 201〜238 は先頭に「1」を付けて移行済み(migration 0017)。
  - `1240`(中学2年・中学生以上)は画面確認用。アンケート欄は検証値(1問目「難しかった」、2問目null)のまま。
- **コース問題数(累積)**: 年中34 / 年長41 / 小学校低学年55 / 小学校高学年61 / 中学生以上68。全年齢群でイラスト・正解ラベルは揃っている。
- **棄却フラグ**運用中(migration 0019。`excluded` / `exclusion_category` / `exclusion_note`)。現在の棄却:
  - 103 = 重複(015と同一児の再受検。初回015を採用)
  - 024 = 重複(011と同一児。回答は011に記録され024は回答0。011を採用)
  - 094 = 途中離脱(年長・3/41問で中断)
  - 011 は「8月に29問→中断、9月に同一児が011で再ログインし残り12問」の接ぎ木だが **採用**(`excluded=false`、note に経緯)
- **アンケート仕様(中学生以上の保護者向け設問)**: 保護者と離れて実施した場合に、保護者向け設問(2問目)を「不明」として省略できるチェックを追加済み(commit `3c11a5e`、`submit-survey` v2 デプロイ済み)。**CSVでは `caregiver_comprehension_rating` の空欄 = 「不明(保護者と離れて実施)」と読む。** 本番で動作確認済み。
- 完了者(フェーズ1の021〜120): 年中18人 / 年長27人。人手採点はまだ0件(AI採点は全件済み)。
- Edge Function・DB・GitHub(main)はすべて反映済み。

### フェーズ2の方針(検討中・未決)

- 今後は、**協力者(保護者など)がインターネット経由でデータを取って送る方式**にする予定。対象は小学生以上。
- **仕組み(ユーザー確認済み・2026-09-21): 「同意書を取ると自動でメールアドレスにID・パスワードが届く仕組み」= Googleフォーム + GAS(Apps Script)の構成で、M4airU で検討した設計案と同じもの。**
  - 流れ: Googleフォーム登録 → GASがスプレッドシートのIDプールから空きIDを自動割当 → 保護者にログイン情報をメール。
  - 設計案・GASテンプレは `id-lists/2026-09-09_フェーズ2_保護者アンケートID運用案.md`(**git管理外・M4airUのみ**)。
  - 先に「使用ツール・IDの取得方法・置き場所」を別PCの記録に見当たらないと書いていたが、上記の構成で解決。スプレッドシートやGASの実体(URL・所在)は未記録。
- **推奨: 未使用IDは再利用せず、`121`〜 を新規発行してフェーズ2専用ブロックにする(案: 100件 = `121`〜`220`)。**
  - 理由1: 未使用IDの紙カードが検査士の手元に残っている。プールにも入れると「1つのIDに別人2人」が起こりうる。`submit-basic-info` は既存IDでも無条件UPDATE(上書き)なので基本情報が潰れ、回答も混ざる。
  - 理由2: 番号帯でフェーズが一目で分かる(フェーズ1=021〜120 / フェーズ2=121〜)。
  - 理由3: 未使用IDをDBに残しても集計に影響しない(基本情報なし・回答0で自然に除外)。
- GASはSupabase APIを使わない(IDは発行済み。スプレッドシートの一覧から空きを配るだけ)。`LockService` で同時送信を排他、プール残10件で通知。
- フォームは同意・連絡先・学年スクリーニングに絞り、詳細な属性はアプリの基本情報画面で入力させる(二重入力回避。age_group判定にアプリ側の基本情報が必要)。
- ログインURL: `https://u10imai.github.io/adoc-s-app/login.html`

### 未決事項

1. (解決済み)「同意書→メールでID・パスワード送付」の仕組みは、上のGAS案と同じもの。残りは、実際に稼働しているスプレッドシート・GASの所在と、プールに載せるIDをどう用意するか。
2. フェーズ2のIDは **新規ブロック発行(推奨)** か **未使用53件(＋001〜020の未使用8件)の再利用** か。
3. 新規なら発行数(既定案100件)。
4. 保護者が基本情報画面で「検査者=保護者」を選ぶよう案内する文言(フォーム確認画面/メール)。
5. 同一保護者の重複送信を弾くか(メールで重複チェック)。
6. `1240` のアンケート欄(検証値)をリセットするか。

### 次にやること

1. 上の未決2(新規発行か再利用か)を決める。未決1はGAS案と同一と確認済み。
2. 新規発行なら、下記「一括発行の手順」でID発行 → スプレッドシート貼り付け用のタブ区切りリスト(ID / パスワード / 状態=未割当)を出力。
3. 配布の仕組み(GAS等)をフォーム項目名に合わせて調整し、テスト送信で動作確認。
4. 020(旧1230)の採点を他の本番データと同様に進める。人手採点はフェーズ1のデータ分も未着手。棄却フラグにより 094・103 は採点一覧に出ない。
5. 追加の除外判断が出たら `excluded / exclusion_category / exclusion_note` を設定(分類: 重複 / 途中離脱 / 技術エラー / その他)。

---

## 別PCでの注意(引き継ぎ)

- 作業前に **`git pull`**。その上で Supabase MCP が接続され、`supabase` CLI がログイン・リンク済みか確認する(プロジェクトID: `puwumxiuhsfmlpfkbbhb`)。
- **本番DBはテスト・本番共用。** テストIDは1000番台、本番は001〜999。
- **`id-lists/` は gitignore 対象(認証情報を含むため)。** M4airU にのみ存在し、別PCには同期されない。ID+パスワードの一覧が必要なときは、DBの `subjects.password_plain` から取得するか、管理者画面「ID発行」タブの発行済み一覧で確認する。
- Claude Code のメモリ(`~/.claude/projects/.../memory/`)は **PCごとのローカル**。同等の内容はこの作業ログにも書く。
- **コミットしないもの**: Mac mini 側の `MEMO.md`、`バックアップ_1230を020へ付替_20260921.json`(生年月日等を含む)。リポジトリは PUBLIC。
- **Edge Functionのデプロイ**: Mac mini では Supabase MCP でデプロイできた(`verify_jwt:false`、`submit-survey` v1→v2)。M4airU では Bash の `supabase functions deploy` が Claude Code の自動許可判定でブロックされたため、ユーザーが手元で `supabase functions deploy <関数名...>` を実行した(M4airU の MCP `deploy_edge_function` は未試行)。
- `execute_sql` は複数文まとめ(特に `begin;/commit;`)がブロックされることがある。1文ずつ実行する。
- PostgREST の Max rows = **1000**。全行を取得する処理は `.range()` ページング(csvExport)か集計RPC(`response_counts_by_subject`)で扱う。
- 削除・付け替えなどデータの件数や場所が変わる操作は、**実行前に必ず実行リストを出してユーザーの確認を取る**(CLAUDE.md のルール)。

## 一括発行の手順(本番ID)

`admin-issue-subject` と同じ方式で、SQL(`execute_sql`)から直接発行できる。

- `subjects` に `(subject_code, password_hash, password_plain, subject_type)` を INSERT。
- `subject_code` は `public.next_subject_code()` を行ごとに呼ぶ(`subject_code_seq`、3桁ゼロ埋め)。
- パスワードは「動物・果物・植物の英単語 + 2桁数字」(例 tiger48)。単語リストは `supabase/functions/admin-issue-subject/index.ts` の `WORDS` と同じ40語。
- ハッシュは pgcrypto: `crypt(pw, gen_salt('bf', 10))`。これは `login` の bcryptjs で検証できることを確認済み。
- 単語は行ごとにランダム選択するため、`(array[...])[1 + floor(random()*40)::int]` の形で書く(サブクエリ `order by random() limit 1` だと1回しか評価されず全行同じ単語になる)。
- 発行後は件数・連番・重複なし・シーケンスの `last_value` を確認する。

## 履歴(新しい順・追記のみ)

### 2026-09-21 15:30 JST / M4airU.local
- やったこと: ユーザーに確認し、Mac mini の記録にある「同意書を取ると自動でメールでID・パスワードが届く仕組み」は M4airU で検討したGAS案と**同じもの**と判明。フェーズ2の未決事項1を解決済みに更新。子どもの愛称が残っている箇所を確認: 現在のファイル・現在のツリーには無く、過去コミット `79789dd`(Mac mini の作業ログ初版)の中にのみ存在。他のファイルには無い。
- 決定事項: フェーズ2の配布の仕組み = Googleフォーム + GAS。
- 注意点: 愛称の履歴からの完全削除には履歴書き換え(force push)が必要。未実施(ユーザーの判断待ち)。

### 2026-09-21 15:20 JST / M4airU.local(統合)
- やったこと: 別PC(Mac mini)の作業ログをpullし、M4airU の作業ログと1つに統合。M4airU 側は一時的に別名へ退避(統合後に削除、ユーザー承認済み)。DBの最新状態(本番120件、テスト39件・1201〜1240、棄却3件、001〜020未使用8件、021〜120未使用53件)を読み取りで再確認して反映。
- 決定事項: 履歴はマシン別ではなく日時順に1本化する。
- 注意点: 統合前の M4airU 版に「次のテストIDは1239」「001〜020の未使用は9件」とあったが、1230→020の付け替え後の正しい値は「1241」「8件」。Mac mini 版の履歴に子どもの愛称が書かれていたため、統合版では「対象児」と伏せた(元の記載は commit `79789dd` の履歴には残っている)。

### 2026-09-21 14:51 JST / M4airU.local
- やったこと: 別PCへの引き継ぎのため、M4airU で作業ログを作成(統合前)。フェーズ1完了状況とフェーズ2方針(推奨・未決)を記録。
- 注意点: リポジトリが PUBLIC のため、氏名・パスワードは書かなかった。

### 2026-09-21 13:55 JST / Mac mini(追記)
- やったこと: 1230→020の付け替えを実行(ユーザー承認済み)。020の空行を削除し、1230の行を番号020・区分本番に更新、パスワードは020の元のものを引き継ぎ、exclusion_note に「テストID1230(2026-08-08検査)から移行」を記録。1つの処理で実行。実行前に該当2行の写しを「バックアップ_1230を020へ付替_20260921.json」に保存(パスワード列なし・未コミット)。
- 検証結果: 020は回答55件(55種すべて)・小学校低学年・アンケート済み・excluded=false。番号1230の行は0件、番号020の行は1件。本番120人のまま、テスト39人(40人から1人減)、連番は120のまま。
- 決定事項: なし
- 注意点: バックアップJSONには生年月日等が入っているため、コミットしないこと。回答データは行のIDで結びついているので移動していない。

### 2026-09-21 13:45 JST / Mac mini
- やったこと: 1230(対象児)を本番IDに付け替える件を相談。本番DBを読み取りのみで確認(書き込みなし)。1230は小2・低学年・55/55問回答済み・アンケート済み・採点0件・除外なし(検査日2026-08-08)。本番は001〜120発行済み(連番は120まで消費、次は121)。020は基本情報・回答とも空の未使用行。
- 決定事項: 付け替え先は020。020は誰にも配布していない(ユーザー確認済み)。8/12の文言微修正はあるが検査手順の変更ではないため、本番データとして混ぜてよい(ユーザー判断)。方針は、1230の行の番号と区分を書き換えて回答55件をそのまま引き継ぐ。020の空行は先に削除が必要(番号の重複不可)。パスワードは020に元々設定のものを残す案。移行元は exclusion_note に記録し、excluded は false のまま。
- 注意点: 121以降への付け替えは連番と衝突するため不可。削除を伴うため実行前に必ず実行リストで確認を取る。

### 2026-09-21 追記4 JST / Mac-mini
- やったこと: ユーザーが本番でテスト完了(問題なし)と報告。
- 決定事項: 本件(中学生以上の保護者向けアンケート省略)は完了。
- 注意点: CSVでは caregiver_comprehension_rating の空欄=「不明(保護者と離れて実施)」と読む。

### 2026-09-21 追記3 JST / Mac-mini
- やったこと: assessment.html・css/style.css・submit-survey/index.ts をコミット(3c11a5e)し origin/main へpush。作業ログとMEMO.mdはコミット対象外。
- 決定事項: なし
- 注意点: 特になし。

### 2026-09-21 追記2 JST / Mac-mini
- やったこと: テストID1240(基本情報入力済み・中学2年・中学生以上、回答20/68枚)で、ヘッドレスChromeによる画面確認。ログイン画面から実際に入り、get-next-questionの応答だけ「全問終了・アンケート未回答」に差し替えてアンケート画面を表示。11項目すべてPASS(チェックで2問目無効化・回答消去・送信可否・外すと復帰・送信後「これで終了です」)。送信は本番submit-surveyに実行され、DBは1問目「難しかった」・2問目nullを確認。
- 決定事項: なし
- 注意点: 確認スクリプトはスクラッチパッド内(プロジェクト外)。1240の回答枚数は20のまま変更なし。

### 2026-09-21 12:44 JST / Mac-mini(追記)
- やったこと: チェック文言を「中学生以上の場合: 保護者と離れて実施したため、不明」に変更。submit-survey をデプロイ(v1→v2、本番版が変更前リポジトリ版と同一であることを事前確認)。テストID1240で確認: (A)1問目のみ・チェックなし→400、(C)2問回答→200で保存、(B)チェックあり→200で2問目null。
- 決定事項: 空欄(null)=不明として扱う。DBスキーマは変更しない。get-next-questionは変更なし。
- 注意点: 1240のアンケート欄は検証で上書き済み(1問目「難しかった」、2問目null)。フロントを先に公開しないこと(今回はfunction先出し済みなのでOK)。

### 2026-09-21 11:18 JST / Mac-mini
- やったこと: origin/main を pull(5コミット遅れだった)。アンケート画面の現行仕様を調査。
- 決定事項: なし
- 注意点: エッジ関数の変更はSupabase MCPでデプロイが必要(verify_jwt:false)。本番DBはテスト・本番共用。

### 2026-09-09 / M4airU.local
- 検査士から「011と024」「015と103」が同一児との連絡 → 調査し、棄却フラグ方式で処理することを決定。
  - 011は8月29問+9月12問の接ぎ木だが採用。015を初回として採用、103を棄却。024は回答0で棄却。094は途中離脱で棄却。
- migration `0019_add_exclusion_columns.sql` を適用(`excluded` / `exclusion_category` / `exclusion_note`、`excluded=true` なら category 必須のCHECK)。
- コード修正: `admin-score-list`(棄却者を採点一覧から除外)、`admin-progress-list` + `admin.html`(棄却バッジ)、`_shared/csvExport.ts`(subjects.csv / responses.csv に棄却列を追加)。commit `36576e9`。デプロイはユーザーが実行し、管理画面で目視確認済み。
- フェーズ2(小学生以上・保護者)のID運用案を検討し、`id-lists/` に保存。推奨は新規ブロック発行(未決)。
- 決定: 不要データは削除せずフラグで除外する(ON DELETE CASCADE で回答も消えるため、物理削除は非推奨)。

### 2026-09-07〜09-08 / M4airU.local
- テストIDを1000番台へ移行(201〜238 → 1201〜1238)。本番IDの一括発行時に旧テストIDと衝突する恐れがあったため。migration `0017`、commit `64870aa`。
- PostgREST 1000件上限で進捗確認・採点サマリの回答数が頭打ちになる不具合を修正(集計RPC `response_counts_by_subject` を追加、migration `0018`、commit `b226cc6`)。CSVエクスポートは修正済みだった(ページング)。
- 本番ID `021`〜`120` を100件一括発行(6人の検査士へ連続ブロックで割当)。パスワードのログイン互換性(pgcrypto→bcryptjs)を確認。配布用一覧は `id-lists/`(gitignore、commit `e284226`)。
- 3日間の本調査を実施し、データ取得完了。
